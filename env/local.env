# Local frontend — API proxied to localhost:3000 (default `npm run dev`)
# Uncomment the next line to hit the deployed Cloud Run API instead:
# VITE_APP_API_URL=https://api-2dvyyijs7a-uc.a.run.app/api
